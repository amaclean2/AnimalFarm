export const PG_WIDTH = 10
export const PG_HEIGHT = 10
export const MAX_HEALTH = 80
export const API_BASE = '/playground'
export const WS_URL = `${location.protocol === 'https:' ? 'wss' : 'ws'}://${location.host}/playground/ws`
